import { UserInfo } from "./types"


export const getUserInfo = () => {
    // TODO: storage에서 가져오기
    return {} as UserInfo
}

export const chatList = () => {
    if (typeof window !== 'undefined') {
        
    }
}